package com.amitgupta.video_player_android_library

/**
 * Acc. to Live or Recorded videos we will show the controller(eg: controller for recorded needs seekbar whereas controller for live does not need it)
 */
enum class PlayType {
    LIVE,
    RECORDED
}
